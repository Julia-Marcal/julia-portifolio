import { HoverLabelDirective } from './hover-label.directive';

describe('HoverLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverLabelDirective();
    expect(directive).toBeTruthy();
  });
});
